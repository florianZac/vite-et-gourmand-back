RewriteEngine On

RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.php [L]
```

**Étape 2 — Modifie ton `Procfile` :**

Remplace :
```
web: heroku-php-apache2 public/
```

Par :
```
web: heroku-php-apache2 -C apache_app.conf public/